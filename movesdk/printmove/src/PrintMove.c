#include "Move.h"
#include <stdio.h>

void move(int speed){
    printf("Robot is now MOVING with speed %d\n", speed);
}

void rotate(int speed){
    printf("Robot is now ROTATING with speed %d\n", speed);
}
